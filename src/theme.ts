import { createTheme } from "@mui/material";

export const generalTheme = createTheme({
    palette: {
        background: {
            default: '#fff'
        },
        primary: {
            main: '#B71C1C'
        }
    }
});